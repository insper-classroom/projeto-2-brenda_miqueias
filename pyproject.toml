[project]
name = "projeto-2-brenda-miqueias"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=25.1.0",
    "mysql-connector-python>=9.6.0",
    "pytest>=9.0.2",
    "python-dotenv>=1.2.2",
]

[tool.pytest.ini_options]
testpaths = ["test"]
pythonpath = ["."]
